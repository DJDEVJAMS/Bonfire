<div>
    <h2>Welcome to the {{hobby_name}} Campfire!</h2>
    <section>
        <h3>Posts</h3>
        {{#each posts}}
        <h4>{{title}}</h4>
       <p>posted by:{{user.username}} </p>
        {{/each}}
    </section>
</div>